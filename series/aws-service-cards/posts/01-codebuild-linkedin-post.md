AWS Series #1 - AWS CodeBuild

AWS CodeBuild is a fully managed build service for compiling source code, running tests, and producing deployment-ready artifacts without maintaining build servers.

The core workflow is defined through `buildspec.yml`, which controls install steps, build commands, test execution, and artifact output for CI/CD pipelines.
