AWS Series #07 - Amazon DynamoDB

Amazon DynamoDB is a serverless NoSQL database for applications that need predictable low-latency access at scale.

The key is designing around access patterns: primary keys, indexes, streams, and global tables shape how the system performs and evolves.
