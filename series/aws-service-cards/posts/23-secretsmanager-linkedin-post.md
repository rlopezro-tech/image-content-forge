AWS Series #23 - AWS Secrets Manager

AWS Secrets Manager helps centrally store, retrieve, rotate, and audit credentials such as database passwords, API keys, OAuth tokens, and application secrets.

Use it to remove hard-coded credentials from applications and manage secret access with encryption, IAM policies, versioning, rotation, and replication.

