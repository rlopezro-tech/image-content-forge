AWS Series #11 - AWS Fargate

AWS Fargate runs containers without managing EC2 instances or server clusters.

Define the container image, task resources, networking, and IAM permissions; AWS manages the underlying compute capacity for ECS or EKS workloads.
