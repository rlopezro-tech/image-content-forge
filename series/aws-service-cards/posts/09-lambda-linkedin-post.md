AWS Series #09 - AWS Lambda

AWS Lambda runs application code in response to events without provisioning or managing servers.

It connects event sources, triggers, runtimes, handlers, execution roles, logs, metrics, and downstream AWS services so teams can build event-driven APIs, automations, file processors, and stream or queue-based workflows.
