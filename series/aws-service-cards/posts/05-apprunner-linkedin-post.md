AWS Series #5 - AWS App Runner

AWS App Runner deploys web applications from source code repositories or container images into managed, scalable services.

It connects application source, build or image configuration, deployment behavior, routing, autoscaling, logs, and IAM permissions into a simpler path for running HTTP workloads on AWS.
