AWS Series #4 - Amazon EC2

Amazon EC2 provides resizable compute capacity through virtual server instances that run in AWS.

It brings together AMIs, instance types, VPC networking, security groups, IAM roles, and storage options such as Amazon EBS to run applications with server-level control.
