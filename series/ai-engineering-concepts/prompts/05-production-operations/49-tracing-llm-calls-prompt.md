# Tracing LLM Calls Card Prompt

Create a single 1080 x 1350 px vertical LinkedIn technical study card for the AI engineering concept **Tracing LLM Calls**.

Category: `05-production-operations`
Badge: `Production Checks`
Output files:

```text
templates/05-production-operations/49-tracing-llm-calls-card.html
images/05-production-operations/49-tracing-llm-calls.png
posts/05-production-operations/49-tracing-llm-calls-linkedin-post.md
```

Visible card text must be English. Use HTML/CSS as the source of truth and render the final PNG from the `.card` element. Do not use remote images, remote fonts, or stock artwork.

Teaching angle: Tracing LLM calls lets you follow a request across prompt assembly, retrieval, tools, model calls, validation, and fallback paths so you can debug problems fast.

Use the default five-section layout with `OVERVIEW`, `HOW IT WORKS`, `PRODUCTION CHECKS`, and `TOOLS / FRAMEWORKS`.

Keep the design clean, technical, readable on mobile, and visually consistent with the AI Engineering Concepts series.
