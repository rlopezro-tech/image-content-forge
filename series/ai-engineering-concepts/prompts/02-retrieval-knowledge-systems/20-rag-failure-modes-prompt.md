# RAG Failure Modes Card Prompt

Create a single 1080 x 1350 px vertical LinkedIn technical study card for the AI engineering concept **RAG Failure Modes**.

Category: `02-retrieval-knowledge-systems`
Badge: `Production Checks`
Output files:

```text
templates/02-retrieval-knowledge-systems/20-rag-failure-modes-card.html
images/02-retrieval-knowledge-systems/20-rag-failure-modes.png
posts/02-retrieval-knowledge-systems/20-rag-failure-modes-linkedin-post.md
```

Visible card text must be English. Use HTML/CSS as the source of truth and render the final PNG from the `.card` element. Do not use remote images, remote fonts, or stock artwork.

Teaching angle: RAG fails when retrieval is weak, context is noisy, filters are wrong, or the sources are stale. The model can only ground answers in the evidence it receives.

Use the default five-section layout with `OVERVIEW`, `HOW IT WORKS`, `PRODUCTION CHECKS`, and `TOOLS / FRAMEWORKS`.

Keep the design clean, technical, readable on mobile, and visually consistent with the AI Engineering Concepts series.
