# Query Rewriting Card Prompt

Create a single 1080 x 1350 px vertical LinkedIn technical study card for the AI engineering concept **Query Rewriting**.

Category: `02-retrieval-knowledge-systems`
Badge: `Implementation`
Output files:

```text
templates/02-retrieval-knowledge-systems/16-query-rewriting-card.html
images/02-retrieval-knowledge-systems/16-query-rewriting.png
posts/02-retrieval-knowledge-systems/16-query-rewriting-linkedin-post.md
```

Visible card text must be English. Use HTML/CSS as the source of truth and render the final PNG from the `.card` element. Do not use remote images, remote fonts, or stock artwork.

Teaching angle: Query rewriting transforms a user's question into a better retrieval query without changing the original intent. The goal is to improve recall and precision before the model answers.

Use the default five-section layout with `OVERVIEW`, `HOW IT WORKS`, `IMPLEMENTATION`, and `TOOLS / FRAMEWORKS`.

Keep the design clean, technical, readable on mobile, and visually consistent with the AI Engineering Concepts series.
